services:
  - name: pocketbase
    type: web
    env: docker
    buildCommand: ""
    startCommand: "./pocketbase serve --http=0.0.0.0:$PORT"
    disk:
      name: pocketbase-data
      mountPath: /pb_data
