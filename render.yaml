services:
  - name: pocketbase
    type: web
    env: linux
    buildCommand: ""
    startCommand: "./pocketbase serve --http=0.0.0.0:$PORT"
    disk:
      name: pocketbase-data
      mountPath: /pb_data
